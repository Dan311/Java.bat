public String endUp(String str); 
      int strLen = str.length();
      int strEnd = (strLen - 3);
      if {strLen <= 3) {
      {
        return str.toUpperCase();
      } else 
        (str.substring((strEnd)) = str.toUpperCase()) {
        return str;
        }
}